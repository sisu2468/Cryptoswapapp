import 'package:flutter/material.dart';

class NetworkModel {
  String name;
  Color color;
  bool isChecked;

  NetworkModel({
    required this.name,
    required this.color,
    required this.isChecked,
  });
}
